#project_name# --> (lumiere)
#version# --> (1.0.0)
#out_dir# --> (../out)
#build_dir# --> (bld)
#sdk_dir# --> (sdk)
#runtime_dir# --> (rt)
#download_dir# --> (dl)
#frameworks_dir# --> (Frameworks)
#manifests_dir# --> (media/Plugins)
#plugin_dir# --> (media/Plugins/bin)
#verbose# --> (true)
#unity_build# --> (true)
#preset# --> (win-avx-dbg)
preset/#display_name# --> (Windows-x64-AVX)
preset/#build_type# --> (debug)
preset/#allow# --> ([avx, debug, windows])
preset/#disallow# --> ([release])
preset/#static_libs# --> (false)
preset/#cppcheck# --> (true)
preset/#cppcheck_suppression# --> (cppcheck_ignore.txt)
preset/#config# --> ([msvc])
preset/config/#compiler_flags# --> ([/std:c++latest, /Zc:__cplusplus, /EHsc, /arch:AVX, /Zc:preprocessor, /source-charset:utf-8, /bigobj])
preset/config/#linker_flags# --> ([/debug, -incremental:no])
preset/#config# --> ([gcc])
preset/config/#compiler_flags# --> ([-fcoroutines, -fno-exceptions, -mavx, -std=c++2a])
#preset# --> (linux-avx-dbg)
preset/#display_name# --> (Linux-x64-AVX)
preset/#build_type# --> (debug)
preset/#allow# --> ([avx, debug, linux])
preset/#disallow# --> ([release])
preset/#static_libs# --> (false)
preset/#config# --> ([msvc])
preset/config/#compiler_flags# --> ([/std:c++latest, /Zc:__cplusplus, /EHsc, /arch:AVX, /Zc:preprocessor, /source-charset:utf-8])
preset/config/#linker_flags# --> ([/debug, -incremental:no])
preset/#config# --> ([gcc])
preset/config/#compiler_flags# --> ([-fcoroutines, -mavx, -std=c++2a])
#preset# --> (windows-avx-asan)
preset/#display_name# --> (Windows-x64-AVX-ASAN)
preset/#build_type# --> (debug)
preset/#allow# --> ([asan, avx, debug, windows])
preset/#disallow# --> (release)
preset/#static_libs# --> (false)
preset/#config# --> ([msvc])
preset/config/#compiler_flags# --> ([/std:c++latest, /Zc:__cplusplus, /EHsc, /arch:AVX, /Zc:preprocessor, /source-charset:utf-8, /fsanitize=address, /bigobj])
preset/config/#linker_flags# --> ([/debug, -incremental:no, /wholearchive:clang_rt.asan_dbg_dynamic-x86_64.lib, /wholearchive:clang_rt.asan_dbg_dynamic_runtime_thunk-x86_64.lib])
preset/#config# --> ([gcc])
preset/config/#compiler_flags# --> ([-fcoroutines, -fsanitize=address, -fsanitize=leak, -mavx, -std=c++2a])
preset/config/#linker_flags# --> ([-fsanitize=address, -fsanitize=leak])
